// hide sert a cacher les éléments
// $("#texte").hide();
$(document).ready(function() {
    // attente du chargement de l'HTML, execution du script
    // $ remplace JQuery : permet la transformation de l'élément sélectionner en objet ou on passe la commande "hide"
    $("#texte").hide();
});